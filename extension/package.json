{
  "name": "browser-bridge-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts src/options.ts --bundle --format=iife --outdir=. --target=chrome116",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
